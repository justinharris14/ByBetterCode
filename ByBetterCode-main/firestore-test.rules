rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules for testing
    // Remove after confirming Firebase connection works
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
