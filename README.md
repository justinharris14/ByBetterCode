# ByBetterCode

events stored to firebase database as well as users saved to auth on firebase.

Still to do:

syncing events to parent dashbaord 
Fix event ID string error(Or just suppress it)
Clean up UI
Add better time and date ui for selecting.
